{{/*TODO autogenerated header*/}}
package {{ .Package }}

import (
{{- range $path, $pkg := .Packages}}
    {{if $pkg.Alias -}}
        "{{$pkg.Path}}" {{$pkg.Alias -}}
    {{- else -}}
        "{{$pkg.Path}}"
    {{- end -}}
{{end}}
)

type archetypeID uint64

const (
    {{range $name, $cmps := .Compositions -}}
        compositionID{{$name}} compositionID = iota{{/*TODO only iota on one*/}}
    {{end}}{{/*TODO fix whitespace here*/}}
)

var (
    {{range $component, $any := .Components}}
    zero{{$component.Name}} {{index .Packages $component.Pkg}}.{{$component.Typ}}
    {{end}}
)
